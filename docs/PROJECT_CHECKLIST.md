# Чеклист планирования проекта

## 📋 Этап 1: Техническое задание

- [ ] **Назначение проекта** — что делает программа (1-2 предложения)
- [ ] **Целевая аудитория** — кто будет использовать
- [ ] **Платформа** — Windows / Linux / macOS / Android
- [ ] **Язык(и)** — Python / C# / другой
- [ ] **Основные функции** — список (5-10 пунктов)
- [ ] **Требования** — производительность, память, диск
- [ ] **Ограничения** — что НЕ делает программа

---

## 📐 Этап 2: Архитектура

### Модули

| Модуль | Назначение | Зависимости |
|--------|------------|-------------|
| core | Ядро программы | — |
| io | Ввод-вывод | core |
| gui | Интерфейс | core, io |
| utils | Утилиты | — |

### Интерфейсы между модулями

```
┌─────────────┐
│     GUI     │
└──────┬──────┘
       │ использует
┌──────▼──────┐
│     IO      │
└──────┬──────┘
       │ использует
┌──────▼──────┐
│    CORE     │
└─────────────┘
```

**Правило:** Зависимости только вниз (GUI → IO → CORE), не наоборот!

---

## 📁 Этап 3: Структура проекта

```
ProjectName/
├── src/
│   ├── __init__.py
│   ├── core/           # Ядро (без зависимостей от GUI/IO)
│   │   ├── __init__.py
│   │   ├── models.py   # Модели данных
│   │   └── logic.py    # Бизнес-логика
│   ├── io/             # Ввод-вывод (зависит от core)
│   │   ├── __init__.py
│   │   ├── reader.py
│   │   └── writer.py
│   ├── gui/            # Интерфейс (зависит от core, io)
│   │   ├── __init__.py
│   │   ├── main.py
│   │   └── widgets/
│   └── utils/          # Утилиты (без зависимостей)
│       ├── __init__.py
│       └── config.py
├── tests/
│   ├── __init__.py
│   ├── test_core/
│   ├── test_io/
│   └── test_gui/
├── docs/
│   ├── conversations/
│   ├── DECISIONS.md
│   └── CONVERSATIONS.md
├── .qwen/
│   └── QWEN.md
├── pyproject.toml
├── requirements-dev.txt
├── README.md
└── .gitignore
```

---

## 🧪 Этап 4: Тестирование

### Обязательные тесты

- [ ] **Ядро (core)** — 100% покрытие критичных функций
- [ ] **Ввод-вывод (io)** — тесты на чтение/запись
- [ ] **Граничные случаи** — пустые данные, максимум, ошибки
- [ ] **Интеграционные** — полный пайплайн работы

### Формат тестов

```python
# tests/test_core/test_merger.py
import pytest
from src.core.merger import merge_layers

class TestMergeLayers:
    """Тесты на объединение слоёв."""
    
    def test_basic_merge(self):
        """Базовое объединение."""
        assert merge_layers([1,2,3,4,5,6], 3) == [2, 5]
    
    def test_empty_input(self):
        """Пустой вход."""
        assert merge_layers([], 3) == []
    
    def test_remainder(self):
        """Остаток от деления."""
        assert merge_layers([1,2,3,4], 3) == [2, 4]
```

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=src --cov-report=html

# Конкретный файл
pytest tests/test_core/test_merger.py -v
```

---

## 📝 Этап 5: Правила кода

### Python

- [ ] Type hints обязательно (PEP 484)
- [ ] Docstring на каждую функцию (Google style)
- [ ] Максимум 500 строк на файл
- [ ] Максимум 5 параметров на функцию
- [ ] Один класс/функция — одна ответственность

### C#

- [ ] Nullable reference types включены
- [ ] XML документация на публичные API
- [ ] Максимум 500 строк на класс
- [ ] Pattern matching где уместно
- [ ] async/await для IO операций

---

## 🔧 Этап 6: Инструменты

### Python

| Инструмент | Назначение | Команда |
|------------|------------|---------|
| ruff | Линтер | `ruff check src/` |
| black | Форматтер | `black src/` |
| mypy | Type checker | `mypy src/` |
| pytest | Тесты | `pytest` |
| pytest-cov | Покрытие | `pytest --cov=src` |

### C#

| Инструмент | Назначение | Команда |
|------------|------------|---------|
| dotnet format | Форматтер | `dotnet format` |
| dotnet test | Тесты | `dotnet test` |
| Roslyn | Анализ | встроено в SDK |

---

## 📦 Этап 7: Сборка и релиз

### Python (.exe)

```batch
:: build.bat
pyinstaller --onefile --windowed src/gui/main.py --name MyApp
```

### C# (.msi / .exe)

```xml
<!-- Publish profile -->
<PublishSingleFile>true</PublishSingleFile>
<SelfContained>true</SelfContained>
```

---

## 🔄 Этап 8: Рабочий процесс

### Начало сессии

1. Открыть задачу в плане
2. Прочитать последний SUMMARY диалога
3. Вспомнить контекст

### Во время работы

1. Писать код с тестами
2. Запускать линтер после каждого файла
3. Коммитить после каждой функции

### Конец сессии

1. Сохранить диалог (SUMMARY + FULL опционально)
2. Обновить план (отметить выполненное)
3. Закоммитить изменения

---

## ✅ Чеклист готовности

- [ ] ТЗ написано
- [ ] Архитектура нарисована
- [ ] Структура создана
- [ ] Инструменты настроены
- [ ] Первый тест написан
- [ ] Линтер проходит
- [ ] Сборка работает

---

*Шаблон создан: 2 апреля 2026 г.*
